# Capped below the next major version on purpose: CI builds the Windows
# executable from these, and a Qt or PyInstaller major release landing
# unannounced is exactly the kind of thing that breaks a build the day you
# need to hand someone a demo.
PySide6>=6.6,<7
pyinstaller>=6.6,<7
