# Windows version resource -- what Explorer shows under Properties > Details.
# Referenced from Lense.spec; ignored on Linux and macOS.
#
# filevers/prodvers must be four plain integers (no room for a word like "alpha"),
# so the readable string lives in FileVersion/ProductVersion below. Keep both in
# step with src/core/version.py.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          '040904B0',
          [StringStruct('CompanyName', 'Lense'),
           StringStruct('FileDescription', 'Lense — CCTV and network design CAD'),
           StringStruct('FileVersion', '1.0 alpha'),
           StringStruct('InternalName', 'Lense'),
           StringStruct('OriginalFilename', 'Lense.exe'),
           StringStruct('ProductName', 'Lense'),
           StringStruct('ProductVersion', '1.0 alpha')]
        )
      ]
    ),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)
